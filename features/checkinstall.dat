output none

command chmod
command cp
command grep
command ln
command mkdir
command mv
command rm
command sed

setint install_commands 0
if install_cmds _command_chmod -a _command_cp -a _command_grep -a \
    _command_ln -a _command_mkdir -a _command_mv -a _command_rm -a \
    _command_sed
  setint install_commands 1
endif

if chk_install_cmds ! _setint_install_commands
 echo
 echo ** ERROR: The install requires the following commands:
 echo **   chmod, cp, grep, ln, mkdir, mv, rm and sed
 echo
 exit
endif
